namespace Corno.Globals.Enums;

public enum ScreenAction
{
    Scan,
    Edit,
    Save,
    Delete,
    Cancel,
    Print,
    Preview,
    Duplicate,
    Search,
    Import,
    Warning,

    // Android
    GetRequisitions,
    GetReasons,
    GetPackets,
    GetCustomers,
    GetGridDataSource,

    // Quality
    GetStoppages,

    GetQuantity,
    GetNextItem,
    SkipItem,

    Transfer,
    Q1,
    Q2,
}