@using Corno.Data.ViewModels
@using Corno.Data.ViewModels.Appointment
@using Corno.Globals.Constants
@using Corno.Services.Bootstrapper
@using Corno.Services.Corno.Masters.Interfaces
@using Kendo.Mvc.UI
@model Corno.Data.Corno.Paper_Setting.Models.Appointment

@{
    ViewBag.Title = "Appointment";

    var collegeService = Bootstrapper.Get<ICollegeService>();
}

<div class="row">
    <div class="box-content">
        <div id="breadcrumb" class="col-md-12">
            <ol class="breadcrumb">
                <li><a href="#">OnlineExam</a></li>
                <li><a href="#">Paper Setting</a></li>
                <li><a href="#">Appointment</a></li>
            </ol>
        </div>
    </div>
</div>

<div class="k-card k-card-vertical">
    <div class="k-card-header k-card-tertiary">
        <div class="row">
            <div class="col-sm-8">
                <h3>Appointments</h3>
            </div>
            <div class="k-card-actions k-card-actions-horizontal k-card-actions-end col-sm-4">
                @(Html.Kendo().Button()
                    .Name("New")
                    .Icon("plus")
                    .SpriteCssClass("k-icon-64")
                    .FillMode(ButtonFillMode.Outline)
                    .ThemeColor(ThemeColor.Info)
                    .Content("Add New")
                    .Events(e => e.Click("onCreate"))
                    .HtmlAttributes(new{@class = "w-100"}))
            </div>
        </div>
    </div>
    <div class="k-card-body">
        <div class="">
            @(Html.Kendo().Grid<AppointmentIndexViewModel>()
                .Name("grid")
                .ToolBar(t => t.Search())
                .Pageable()
                .Sortable()
                .Filterable(f => f.Mode(GridFilterMode.Row	))
                .Resizable(resize => resize.Columns(true)) // Allow column resizing
                .PersistSelection()
                .HtmlAttributes(new { @class = "w-100" })
                .Columns(columns =>
                {
                    columns.Bound(m => m.Id)
                        .HtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .Title("Code")
                        .Width(10);
                    columns.Bound(m => m.InstanceId)
                        .HtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .Title("Instance");
                    columns.Bound(m => m.CollegeName)
                        /*.ClientTemplate(@<text>
                                            var collegeName = collegeService.GetViewModel(m.CollegeId).Name
                                         </text>)*/
                        .HtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .Title("College");
                    columns.Bound(m => m.CourseName)
                        .HtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .Title("Course");
                    columns.Bound(m => m.CoursePartName)
                        .HtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .Title("Course Part");
                    columns.Bound(m => m.SubjectName)
                        .HtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .ClientGroupHeaderTemplate("<span style='font-weight:bold;'>Subject :</span> <span style='font-weight:normal;'>#= value #</span>")
                        .Title("Subject");
                    columns.Command(command =>
                    {
                        command.Custom("Edit").Text("Edit").IconClass("k-icon k-i-pencil")
                            .HtmlAttributes(new { @class = "k-grid-edit" })
                            .Visible("function(dataItem) { return dataItem.Status === 'Active'; }"); // Show only if Status is "Active";
                    })
                        .HeaderHtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .HtmlAttributes(new { @class = "k-text-center !k-justify-content-center" })
                        .Width(80)
                        .Title("Actions"); // Add a class to identify the command column;

                })
                .Events(events => events.DataBound("onDataBound"))
                .DataSource(dataSource => dataSource
                    .Ajax()
                    .Read(read => read.Action("GetIndexViewModels", "Appointment"))
                    .Sort(sort => sort.Add(ModelConstants.Id).Descending()) // Initial sort by Id in ascending order
                    /*.Group(group =>
                    {
                        group.Add(g => g.FacultyName);
                        group.Add(g => g.CourseName);
                    })*/
                    .Model(model => model.Id(ModelConstants.Id))
                ))
        </div>
    </div>
</div>


<script>
    function onCreate() {
        window.location.href = '/Appointment/Create';
    }

    function onDataBound(e) {
        var grid = $("#grid").data("kendoGrid");
        // Auto-fit each column
        grid.columns.forEach(function (column, index) {
            grid.autoFitColumn(index);
        });

        $(".k-grid-edit").on("click", function () {
            var grid = $("#grid").data("kendoGrid");
            var row = $(this).closest("tr");
            var dataItem = grid.dataItem(row);

            if (dataItem) {
                var id = dataItem.Id; // Assuming Id is the unique identifier of the item
                window.location.href = '/Appointment/Edit/' + id; // Replace with your edit page URL
            }
        });
    }

    function NumericFilter(args) {
        args.element.kendoNumericTextBox({
            "spinners": true,
            "format": "n0",
            "decimals": 0,
        });
    }

</script>